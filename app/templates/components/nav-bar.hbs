<nav id='navbar' class="navbar navbar-expand-lg navbar-light bg-light justify-content-between">
  {{#link-to 'home' class='navbar-brand'}}
    Mantock
  {{/link-to}}
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav">
      {{#link-to 'home' tagName='li'}}
        <li class="nav-item">
          <a class="nav-link {{if (eq currentRoute 'home') 'active'}}">Home <span class="sr-only">(current)</span></a>
        </li>
      {{/link-to}}
      {{#link-to 'projects' tagName='li'}}
        <li class="nav-item">
          <a class="nav-link {{if (eq currentRoute 'projects') 'active'}}">Projects <span class="sr-only">(current)</span></a>
        </li>
      {{/link-to}}
      {{#link-to 'resume' tagName='li'}}
        <li class="nav-item">
          <a class="nav-link {{if (eq currentRoute 'resume') 'active'}}">Resume <span class="sr-only">(current)</span></a>
        </li>
      {{/link-to}}
    </ul>
  </div>

  {{yield}}

</nav>
